# AI 학습계획 추천기 상세 시나리오

## 1. 서비스 개요

### 1-1. 서비스명
**AI 학습계획 추천기**

### 1-2. 서비스 한 줄 설명
사용자가 자신의 학습 목표, 현재 수준, 하루 공부 가능 시간, 학습 기간을 입력하면  
AI가 이에 맞는 **맞춤형 학습 계획**, **우선순위**, **학습 팁**, **추천 검색 키워드**를 제공하는 웹서비스이다.

### 1-3. 해결하려는 문제
많은 학습자는 다음과 같은 어려움을 겪는다.

- 무엇부터 공부해야 할지 모른다.
- 학습 계획을 스스로 세우기 어렵다.
- 하루 공부 시간이 제한적이다.
- 목표는 있지만 구체적인 실행 계획이 없다.

이 서비스는 사용자의 상황을 입력받아 **실행 가능한 학습 계획을 빠르게 제안**함으로써 이러한 문제를 해결한다.

---

## 2. 과제 목표 및 요구사항 반영

### 2-1. 목표 반영
본 서비스는 단순한 정보 제공 페이지가 아니라, 사용자의 입력을 바탕으로 AI가 실제로 도움이 되는 결과를 생성하는 **문제 해결형 AI 웹서비스**를 목표로 한다.

### 2-2. 기능 요구사항 반영
다음 요구사항을 반영한다.

- 3페이지 이상 구성
- 사용자 입력 폼 제공
- AI API 연동
- Python API 사용
- 반응형 웹 구현
- 오류 처리 포함
- 배포 가능한 구조 설계

### 2-3. 보너스 과제 반영
완성도를 높이기 위해 다음 요소도 포함한다.

- 로딩 상태 안내
- 예외 상황별 사용자 메시지 처리
- 예시 입력 기능
- 결과 복사 기능
- 검색 키워드 + 고정 참고 링크 제공
- 모바일 사용성을 고려한 반응형 UI

---

## 3. 주요 사용자

### 3-1. 사용자 A: 초보 대학생
- 목표: Python 기초 배우기
- 수준: 입문
- 하루 공부 시간: 1시간
- 기간: 4주
- 니즈: 무엇부터 시작해야 하는지 알고 싶다.

### 3-2. 사용자 B: 취업 준비생
- 목표: SQL 및 데이터 분석 기초 학습
- 수준: 초급
- 하루 공부 시간: 2시간
- 기간: 6주
- 니즈: 우선순위가 있는 현실적인 계획이 필요하다.

### 3-3. 사용자 C: 직장인
- 목표: 프론트엔드 기초 학습
- 수준: 입문
- 하루 공부 시간: 30분
- 기간: 8주
- 니즈: 짧은 시간 기준의 부담 없는 계획이 필요하다.

---

## 4. 페이지 구성

과제의 **3페이지 이상** 조건을 충족하기 위해 다음과 같이 구성한다.

### 4-1. 홈 페이지
#### 목적
- 서비스 소개
- 사용 방법 간단 안내
- 시작 버튼 제공

#### 주요 내용
- 서비스명
- 서비스 설명
- 입력값과 결과 예시
- `학습 계획 추천받기` 버튼

#### 사용자 행동
1. 사용자는 홈 페이지에 접속한다.
2. 서비스 소개를 읽고 어떤 기능인지 이해한다.
3. 추천받기 버튼을 눌러 입력 페이지로 이동한다.

---

### 4-2. 입력 페이지
#### 목적
사용자의 학습 정보를 입력받는다.

#### 입력 항목
- 학습 목표
- 현재 수준
- 하루 공부 가능 시간
- 학습 기간

#### 선택 입력 항목(보너스 기능)
- 선호 학습 방식(이론 중심 / 실습 중심 / 혼합)
- 추가 요청사항(예: 주말 중심, 취업 준비용 등)

#### 주요 UI 요소
- 입력 폼
- 드롭다운 또는 선택 버튼
- 예시 입력 버튼
- 초기화 버튼
- 추천받기 버튼

#### 사용자 행동
1. 사용자는 학습 목표를 입력한다.
2. 현재 수준을 선택한다.
3. 하루 공부 가능 시간을 선택한다.
4. 학습 기간을 선택한다.
5. 추천받기 버튼을 누른다.
6. 시스템은 입력값을 검증한 뒤 API 요청을 보낸다.

---

### 4-3. 결과 페이지
#### 목적
AI가 생성한 맞춤형 학습 계획을 사용자에게 보여준다.

#### 표시 내용
- 학습 계획 요약
- 주차별 또는 단계별 계획
- 학습 우선순위
- 학습 팁
- 추천 검색 키워드
- 고정 참고 링크
- 다시 추천받기 버튼
- 결과 복사하기 버튼

#### 사용자 행동
1. 사용자는 추천 결과를 확인한다.
2. 계획을 읽고 자신의 상황에 맞는지 판단한다.
3. 검색 키워드와 참고 링크를 활용해 추가 학습 자료를 찾는다.
4. 필요하면 다시 추천받기를 눌러 입력을 수정한다.

---

### 4-4. 이용 가이드 페이지
#### 목적
서비스 사용 방법과 결과 해석 방법을 안내한다.

#### 주요 내용
- 입력 항목 설명
- 추천 결과 활용 방법
- AI 결과는 참고용이라는 안내
- 자주 묻는 질문(FAQ)

#### 기대 효과
- 페이지 수 요건을 안정적으로 충족할 수 있다.
- 서비스 완성도를 높일 수 있다.

---

## 5. 핵심 사용자 시나리오

### 5-1. 정상 이용 시나리오

#### 상황
사용자는 Python을 처음 배우고 싶지만 어떤 순서로 공부해야 할지 모른다.

#### 흐름
1. 사용자는 홈 페이지에 접속한다.
2. 서비스 소개를 읽고 맞춤형 계획 추천 서비스임을 이해한다.
3. `학습 계획 추천받기` 버튼을 눌러 입력 페이지로 이동한다.
4. 다음 정보를 입력한다.
   - 목표: Python 기초
   - 현재 수준: 입문
   - 하루 공부 시간: 1시간
   - 학습 기간: 4주
5. 추천받기 버튼을 누른다.
6. 프론트엔드는 입력값 유효성 검사를 수행한다.
7. 입력값에 문제가 없으면 Python API에 POST 요청을 보낸다.
8. Python API는 입력값을 정리하여 AI API에 전달한다.
9. AI는 맞춤형 학습 계획을 생성한다.
10. Python API는 결과를 JSON 형식으로 정리해 프론트엔드에 반환한다.
11. 결과 페이지에 다음 정보가 표시된다.
    - 학습 요약
    - 1주차 ~ 4주차 계획
    - 우선순위
    - 학습 팁
    - 추천 검색 키워드
    - 참고 링크
12. 사용자는 결과를 보고 학습을 시작한다.

#### 기대 결과
- 초보자도 빠르게 학습을 시작할 수 있다.
- 막연한 목표를 구체적인 계획으로 바꿀 수 있다.

---

### 5-2. 입력 누락 시나리오

#### 상황
사용자가 학습 목표를 입력하지 않고 추천받기 버튼을 누른다.

#### 흐름
1. 사용자는 입력 페이지에서 목표를 비워 둔다.
2. 다른 항목만 선택한 뒤 추천받기 버튼을 누른다.
3. 프론트엔드는 입력값 검사를 수행한다.
4. 목표가 비어 있으므로 API 요청을 보내지 않는다.
5. 화면에 오류 메시지를 표시한다.
   - 예: `학습 목표를 입력해주세요.`
6. 목표 입력 칸을 강조 표시한다.
7. 사용자는 목표를 입력한 후 다시 추천받기를 시도한다.

#### 목적
- 잘못된 요청을 미리 차단한다.
- 사용자가 수정할 위치를 쉽게 알 수 있게 한다.

---

### 5-3. 로딩 시나리오

#### 상황
AI 응답 생성에 시간이 걸린다.

#### 흐름
1. 사용자가 정상적으로 입력 후 추천받기를 요청한다.
2. 추천받기 버튼은 비활성화된다.
3. 화면에 로딩 메시지와 스피너를 표시한다.
   - 예: `AI가 맞춤형 학습 계획을 생성하고 있습니다.`
4. 일정 시간 이상 걸릴 경우 추가 메시지를 보여준다.
   - 예: `응답이 다소 지연되고 있습니다. 잠시만 기다려주세요.`
5. 응답이 도착하면 결과 페이지로 이동한다.

#### 목적
- 사용자가 서비스가 멈췄다고 오해하지 않도록 한다.
- 사용자 경험을 향상시킨다.

---

### 5-4. API 오류 시나리오

#### 상황
AI API 호출 또는 Python API 처리 중 오류가 발생한다.

#### 흐름
1. 사용자가 입력을 마치고 추천받기를 요청한다.
2. API 호출 중 서버 오류가 발생한다.
3. 시스템은 오류를 감지한다.
4. 결과 대신 오류 메시지를 표시한다.
   - 예: `추천 결과를 불러오는 중 문제가 발생했습니다.`
   - 예: `잠시 후 다시 시도해주세요.`
5. 사용자에게 다음 선택지를 제공한다.
   - 다시 시도하기
   - 입력 페이지로 돌아가기
6. 개발자 측에서는 콘솔 또는 서버 로그에 오류 원인을 기록한다.

#### 목적
- 오류 발생 시 화면이 깨지지 않도록 한다.
- 재시도 가능한 흐름을 제공한다.

---

### 5-5. 모바일 이용 시나리오

#### 상황
사용자가 스마트폰에서 서비스에 접속한다.

#### 흐름
1. 사용자는 모바일 브라우저에서 홈 페이지를 연다.
2. 화면 폭에 맞춰 콘텐츠가 세로 배치된다.
3. 입력 폼은 한 줄에 하나의 항목이 보이도록 정렬된다.
4. 버튼은 손가락으로 누르기 쉬운 크기로 표시된다.
5. 결과 페이지는 카드형 UI 또는 섹션 분리 UI로 구성된다.
6. 긴 텍스트는 자동 줄바꿈 처리되어 읽기 쉽게 표시된다.
7. 사용자는 스크롤을 통해 결과 전체를 쉽게 확인한다.

#### 목적
- 반응형 웹 요구사항을 충족한다.
- 모바일 환경에서도 사용성을 유지한다.

---

## 6. 시스템 시나리오

### 6-1. 프론트엔드 처리 흐름
1. 사용자가 폼을 입력한다.
2. 프론트엔드는 유효성 검사를 수행한다.
3. 문제가 없으면 `fetch`로 POST 요청을 보낸다.
4. 로딩 상태를 화면에 표시한다.
5. 응답이 성공하면 결과 데이터를 렌더링한다.
6. 응답이 실패하면 오류 메시지를 표시한다.

### 6-2. 백엔드(Python API) 처리 흐름
1. `/api/study-plan` 엔드포인트가 요청을 받는다.
2. 서버에서 입력값을 검증한다.
3. 입력값을 기반으로 AI 프롬프트를 생성한다.
4. AI API를 호출한다.
5. 응답 결과를 구조화된 JSON으로 가공한다.
6. 프론트엔드에 결과를 반환한다.

### 6-3. AI 처리 흐름
AI는 다음 기준에 따라 결과를 생성한다.

- 사용자의 목표를 반영한다.
- 현재 수준에 맞는 난이도로 계획을 제안한다.
- 하루 공부 가능 시간을 고려해 분량을 조정한다.
- 학습 기간에 맞춰 주차별 계획을 만든다.
- 초보자도 이해하기 쉬운 문장으로 작성한다.
- 링크 환각을 줄이기 위해 직접 URL을 생성하기보다 검색 키워드 중심으로 제공한다.

---

## 7. 입력 및 출력 예시

### 7-1. 입력 예시

```json
{
  "goal": "Python 기초 학습",
  "level": "입문",
  "dailyTime": "1시간",
  "duration": "4주"
}
```

### 7-2. 출력 예시

```json
{
  "summary": "4주 동안 Python 기초 문법과 간단한 실습을 중심으로 학습합니다.",
  "plan": [
    "1주차: 변수, 자료형, 입출력",
    "2주차: 조건문, 반복문",
    "3주차: 함수, 리스트, 딕셔너리",
    "4주차: 간단한 프로젝트와 복습"
  ],
  "priorities": [
    "기초 문법 이해",
    "짧은 실습 반복",
    "마지막 주 복습 및 응용"
  ],
  "tips": [
    "하루 1시간이라면 이론 30분, 실습 30분으로 나누는 것이 좋습니다.",
    "매주 마지막 날에는 복습 시간을 따로 확보하세요."
  ],
  "keywords": [
    "python beginner tutorial",
    "python basic exercises"
  ],
  "resourceCategory": "python"
}
```
### 7-3. 프론트엔드에서 보여줄 참고 링크 예시

- Python 공식 문서
- W3Schools Python
- MDN Web Docs
- SQLBolt
- freeCodeCamp

> 실제 표시 링크는 학습 주제별로 미리 검증된 링크를 매핑해서 제공한다.

---

## 8. 기능 요구사항 대응표

| 과제 요구사항 | 반영 방식 |
|---|---|
| 3페이지 이상 | 홈 / 입력 / 결과 / 가이드 페이지 구성 |
| AI API 연동 | 입력 후 Python API → AI API 호출 구조 |
| 기능성 있는 서비스 | 사용자 맞춤형 학습 계획 추천 |
| 반응형 웹 | 모바일 시나리오 및 반응형 UI 설계 |
| 사용자 입력 처리 | 목표, 수준, 시간, 기간 입력 폼 제공 |
| 결과 출력 | 계획, 우선순위, 팁, 검색 키워드 제공 |
| 오류 처리 | 빈 입력, API 오류, 응답 지연 처리 |
| 배포 가능 구조 | 프론트 + Python API 분리, Vercel 배포 고려 |

---

## 9. 보너스 과제 반영 시나리오

### 9-1. 예시 입력 자동 채우기

1. 사용자가 입력 페이지에서 `예시로 체험하기` 버튼을 누른다.
2. 폼에 예시 값이 자동 입력된다.
3. 사용자는 빠르게 추천 과정을 체험할 수 있다.

#### 기대 효과

- 시연이 쉬워진다.
- 처음 사용하는 사용자도 이용 방법을 바로 이해할 수 있다.

---

### 9-2. 결과 복사하기

1. 사용자가 결과 페이지에서 `계획 복사하기` 버튼을 누른다.
2. 추천 결과가 클립보드에 복사된다.
3. 사용자는 노션, 메모장, 문서 등에 붙여넣어 활용할 수 있다.

#### 기대 효과

- 실용성을 높일 수 있다.
- 추천 결과를 바로 저장해서 활용할 수 있다.

---

### 9-3. 다시 추천받기

1. 사용자가 결과를 본 후 계획이 마음에 들지 않는다고 느낀다.
2. `다시 추천받기` 버튼을 누른다.
3. 입력 페이지로 돌아가 일부 조건을 수정한다.
4. 새로운 결과를 다시 생성한다.

#### 기대 효과

- 단일 결과에 고정되지 않고 다양한 계획을 비교할 수 있다.

---

## 10. 사용자 경험(UX) 설계 포인트

### 10-1. 쉬운 입력

- 드롭다운, 버튼형 선택 UI를 활용한다.
- 예시 플레이스홀더를 제공한다.

### 10-2. 명확한 결과

- 긴 문단보다 카드형, 목록형 결과를 우선한다.
- 주차별 계획을 구분해서 보여준다.

### 10-3. 친절한 예외 처리

- 빈 입력 시 즉시 안내한다.
- 로딩 상태를 명확히 보여준다.
- 오류 시 재시도 방법을 제시한다.

### 10-4. 모바일 친화성

- 버튼 크기 확대
- 세로 레이아웃 중심
- 결과 카드 간격 확보

---

## 11. 최종 시나리오 요약

AI 학습계획 추천기는 사용자가 자신의 학습 목표와 상황을 입력하면 AI가 이에 맞는 계획을 제안하는 맞춤형 학습 지원 서비스이다.

사용자는 홈 페이지에서 서비스를 이해하고, 입력 페이지에서 정보를 입력한 뒤, 결과 페이지에서 개인 맞춤형 계획과 팁을 확인한다.

서비스는 다음을 만족한다.

- 3페이지 이상 구성
- AI API 연동
- Python API 사용
- 반응형 웹 구현
- 빈 입력 / 오류 / 지연 처리
- 보너스 기능 반영 가능

이를 통해 사용자는 막연한 학습 목표를 실행 가능한 구체적인 계획으로 바꿀 수 있다.
